# strategy-discovery-m1 — Ledger

Single source of truth for execution state. Sections are owned by different skills —
the planner seeds Plan + Phases; the decomposer fills Steps per phase; the supervisor
updates Steps and appends Revisions.

## Plan

- plan-name: strategy-discovery-m1
- current-phase: 4 complete (last phase; all 24 steps done — milestone ready for the lead-developer's evaluation)
- working-branch: milestone/1-evaluation-harness
- starting-commit: 48c97c85656ec6ad0d8995fd5258168288198b02
- default-branch: develop
- artifacts-dir: implementation-artifacts/

## Phases

| Phase | Status  | Notes |
| ----: | ------- | ----- |
| 1     | complete | All 7 steps done (01, 02, 03, 04, 05, 06-gate, 07-float-roundtrip). Phase 1 DoD re-verified by the supervisor: 15/15 gate items PASS, 5 quality gates exit 0, `cargo test --workspace` 19 ok / 0 FAILED, invariants 7/7, warm debug wall 33.19 s (budget 40 s), pinned run_ids reproduce. Contracts and hardening: dev-deps + proptest invariants, schema consts + record types, `GameBundle.roster`, logging wart, serde_json `float_roundtrip` + ADR 0013. |
| 2     | complete | All 6 steps done (08-scaffold, 09-tournament, 10-archive, 11-evaluate, 12-tournament-bench, 13-gate); no revisions, no retries, no escalations. Phase 2 DoD re-verified by the supervisor on the integrated tree: gate report 14/14 items PASS (`Overall verdict: PASS`); 5 quality gates exit 0 (`check --workspace --all-targets`, `fmt --all --check`, `clippy --workspace --all-targets --all-features -- -D warnings`, `doc --no-deps --workspace` 0 warnings); `cargo test --workspace` exit 0, 20 `test result: ok` / 0 FAILED (19 → 20, the new `tournament_bench` binary); unit tests tournament 7/7, evaluate 6/6, archive 6/6; bench prints 6 `[bench]` lines (5 tournament with `games/s=` + `us/move=` + `threads=`, 1 agreement `positions/s=` at `positions=390 rate=1.000`), `tournament_bench` finishes in 3.34 s; warm debug `cargo test --workspace` wall 38.95 s (budget 50 s); re-exports 3 new lines / 13 `pub use` total; game-name rule holds on all three new modules; pinned run_ids reproduce (`cli_pipeline` 8 passed, `corpus` 10 passed) and `git diff dcd22b1 --name-only -- src/cli docs/plan.md configs src/io` is still exactly the three Phase 1 files. Emergent-contract note for Phase 3: `AnnotationSet<G>` deliberately has NO derives (`AnnRec<G>` is generic), so `Result<AnnotationSet<G>, _>::unwrap_err()` does not compile — match on the error instead. Acceptance-pattern note for the decomposer: step 12's bare `grep -c "cfg!(debug_assertions)" tests/tournament_bench.rs → 1` also matches prose in a module doc, so it constrains documentation as well as code; anchor such patterns to a code line next time. |
| 3     | complete | All 7 steps done (14-evaluate-cli, 15-cli-evaluate-tests, 16-adr-0011, 17-adr-0012, 18-adding-a-game, 19-readme-claude, 20-gate); no revisions, no retries, no escalations; two waves exactly as decomposed ({14,16,17,18} then {15,19}), gate run in-tree. Phase 3 DoD re-verified by the supervisor on the integrated tree at `d38ba45`: gate report 16/16 items PASS (`Overall verdict: PASS`); 5 quality gates exit 0 (`check --workspace --all-targets`, `fmt --all --check`, `clippy --workspace --all-targets --all-features -- -D warnings`, `doc --no-deps --workspace` 0 warnings); `cargo test --workspace` exit 0, 21 `test result: ok` / 0 FAILED (20 -> 21, the new `cli_evaluate` binary); `cargo test --test cli_evaluate` = `ok. 4 passed` (all four named tests) in 4.64 s; warm debug `cargo test --workspace` wall 41.61 s (budget 60 s); the game-name awk rule passes on `src/cli/evaluate.rs` and on every non-`src/games/` file naming a game, and `git diff dcd22b1 --name-only -- src/cli` is exactly `evaluate.rs`, `logging.rs`, `mod.rs` (neither `games.rs` nor `error.rs`); `docs/adr` holds 13 files, ADR 0011/0012 pass the `# ADR 001N: ` / 4-section / `Accepted — 2026-08-22 (Milestone 1 / plan Phase 7)` / heading / `|---` checkers; `docs/adding-a-game.md` has 10 `## Step N:` sections, `## Worked illustration: Konane` and `## Second-game dry-run checklist` with 9 `- [ ]` items; README has `### evaluate` (11 `### ` sections), passes the markdown checkers, and its stdout block diffs zero against a live full-scale run modulo the `out=` echo; `CLAUDE.md` Commands line lists `evaluate`; `runs/` absent from the working tree. Step 14 re-verified end to end by the supervisor in its worktree: the pinned test-scale stdout reproduced byte for byte (`bce1c9d8c566106e` with annotations, `e0368cde740bd9c2` without), byte-identity across repeat / `-v` / `--serial` / `--threads 2` (single SHA-256 for `evaluation.json`, `archive.json`, `entries.jsonl`), `heuristic-rules` exit 2 with nothing written, `--strict` exit 3 after writing, all four usage errors exit 2. Full-scale figures reproduced by the supervisor's own live run: `perfect` 280 games 87/193/0 agreement 1.000, `random` loss_rate 0.875 agreement 0.579, `depth-3` loss_rate 0.150 agreement 0.978, `evaluation=44ad0fffbef146da roster=ttt-benchmark-v1 archive_entries=3`. Note for Phase 4: step 18's `docs/adding-a-game.md` is 407 lines versus the step context's 200-350 target (only a `>= 150` floor is checked); the worker recorded this as its single deviation and it is DoD-neutral. |
| 4     | complete | All 4 steps done (21-determinism, 22-throughput, 23-gate-c-record, 24-gate); four serial waves exactly as decomposed, every step run in the integrated checkout (no worktrees — each wave was a lone ready step, and 21/22 reuse the debug/release build caches per the run-location advice); no retries, no escalations, no decomposer round-trips; one in-flight step-file correction (step 22, see Revisions). Phase 4 DoD re-verified by the supervisor on the integrated tree at `05cedb5`: all three evidence files carry the standard eight-field header (`tournament-determinism.md` 8 rows, `tournament-throughput.md` 8 rows, `tournament-throughput.json` `header` 8 fields in order `date,git_commit,rustc,os,cpu,ram,build_profile,command`); `tournament-determinism.md` has 6 `identical: YES` (budget >= 4) and 0 `identical: NO`, `all identical: YES`; the `.json` parses with `ConvertFrom-Json` (top level `header,config,tournament_bench,tournament_bench_agreement_corpus,evaluate_cli,agreement_exhaustive,evaluate_full`) and every `.md` number was cross-checked cell by cell against it by the supervisor (5 bench rows, 6 CLI rows, agreement row, evaluate-wall row — all exact); `docs/component-evaluation.md` ends `Gate C verdict: PASS` and `git diff dcd22b1 -U0` on it is the single pure-addition hunk `@@ -156,0 +157,` with 0 removed lines; the project gate report lists 12 `### Item` sections under `## Project Definition of Done` plus items 13-14 under `## Phase 4 Definition of Done`, 14 `verdict: PASS` / 0 `verdict: FAIL`, last line `Overall verdict: PASS`; supervisor's own live quality-gate run: `cargo test --workspace` exit 0 with 21 `test result: ok` / 0 FAILED, `cargo fmt --all --check` exit 0, `cargo clippy --workspace --all-targets --all-features -- -D warnings` exit 0, `cargo doc --no-deps --workspace` exit 0 with 0 warnings, warm debug workspace wall 42.56 s (budget 60 s). Evidence ground-truthed rather than trusted: the determinism artifact's recorded SHA-256s and byte sizes were re-derived from the live `target/m1/determinism/run-1` outputs (`evaluation.json` 38924 B `1487b18b...`, `archive.json` 429 B, `entries.jsonl` 15448 B / 3 lines — matching the decomposer's dry-run pins), and the embedded scripts diff byte-for-byte against the scripts that ran. Measured Gate C figures (release, medians of 3): bench serial 268.1 games/s / 446.6 us/move, threads 2/4/8 = 499.7/923.6/1400.1 games/s, global pool 1560.6 games/s (speedup 6.03x); `evaluate` 4200 games serial 6.991 s (600.8 games/s), default pool 1.112 s (3777.0 games/s); agreement over 13560 exhaustive positions 0.053 s (255849 positions/s); canonical `--games 100 --annotations` 1.115 s; `Same order of magnitude: YES` versus Phase 5 corpus generation (736.0 / 4651.8). Notes for the lead-developer's milestone evaluation: (i) the gate report's item 12 records the committed-worker-report count (`git ls-files` over `implementation-artifacts/strategy-discovery-m1-NN-*-report.md`, piped to `wc -l`) as 23, correct at the moment it ran (pre-commit); after step 24's own commit the same command returns 24, and the ledger `done` count is now 24 — both expected, not drift; (ii) step 23's Gate C row 1 honestly cites the LOCAL run of the CI command set, since this branch reaches `develop` by squash merge and CI runs after that; (iii) step 23 recorded "18 integration binaries" where the step template said 19 — the supervisor confirmed `ls tests/*.rs` is exactly 18 (1 lib + 1 bin + 18 integration + 1 doctest = the 21 `test result: ok` lines), so the template figure was wrong and the artifact is right. |

## Steps

<!-- decomposer fills per phase: id | phase | status | files | commit,
     plus a "Phase <N> notes" block: dependency graph, couplings, emergent contracts -->

| id | phase | status | files | commit |
| --- | --- | --- | --- | --- |
| 01-dev-deps | 1 | done | Cargo.toml, Cargo.lock, implementation-artifacts/strategy-discovery-m1-01-dev-deps-report.md | 8116feb |
| 02-invariants | 1 | done | tests/invariants.rs, tests/invariants.proptest-regressions, implementation-artifacts/strategy-discovery-m1-02-invariants-report.md | 41814c7 |
| 03-schema-records | 1 | done | src/io/schema.rs, src/io/mod.rs, implementation-artifacts/strategy-discovery-m1-03-schema-records-report.md | 9afef5d |
| 04-bundle-roster | 1 | done | src/discovery/bundle.rs, src/games/tictactoe/corpus.rs, implementation-artifacts/strategy-discovery-m1-04-bundle-roster-report.md | 62d57c3 |
| 05-logging-fix | 1 | done | src/cli/logging.rs, implementation-artifacts/strategy-discovery-m1-05-logging-fix-report.md | edf028c |
| 06-gate | 1 | done | implementation-artifacts/strategy-discovery-m1-06-gate-report.md | 34055a7 |
| 07-float-roundtrip | 1 | done | Cargo.toml, Cargo.lock, docs/adr/0013-serde-json-float-roundtrip.md, implementation-artifacts/strategy-discovery-m1-07-float-roundtrip-report.md | 13418ba |
| 08-scaffold | 2 | done | src/discovery/mod.rs, src/discovery/tournament.rs, src/discovery/evaluate.rs, src/discovery/archive.rs, tests/fixtures/roster-tiny.toml, implementation-artifacts/strategy-discovery-m1-08-scaffold-report.md | 5bbc603 |
| 09-tournament | 2 | done | src/discovery/tournament.rs, implementation-artifacts/strategy-discovery-m1-09-tournament-report.md | f226120 |
| 10-archive | 2 | done | src/discovery/archive.rs, implementation-artifacts/strategy-discovery-m1-10-archive-report.md | 7943705 |
| 11-evaluate | 2 | done | src/discovery/evaluate.rs, src/discovery/mod.rs, implementation-artifacts/strategy-discovery-m1-11-evaluate-report.md | 672a4b8 |
| 12-tournament-bench | 2 | done | tests/tournament_bench.rs, implementation-artifacts/strategy-discovery-m1-12-tournament-bench-report.md | df20a05 |
| 13-gate | 2 | done | implementation-artifacts/strategy-discovery-m1-13-gate-report.md | 09d971c |
| 14-evaluate-cli | 3 | done | src/cli/evaluate.rs, src/cli/mod.rs, tests/fixtures/strategies-eval.toml, tests/fixtures/strategies-heuristic.toml, implementation-artifacts/strategy-discovery-m1-14-evaluate-cli-report.md | 017d0db |
| 15-cli-evaluate-tests | 3 | done | tests/cli_evaluate.rs, implementation-artifacts/strategy-discovery-m1-15-cli-evaluate-tests-report.md | 9d4179e |
| 16-adr-0011 | 3 | done | docs/adr/0011-strategy-evaluation-harness.md, implementation-artifacts/strategy-discovery-m1-16-adr-0011-report.md | 6ab2b8c |
| 17-adr-0012 | 3 | done | docs/adr/0012-strategy-archive-and-novelty.md, implementation-artifacts/strategy-discovery-m1-17-adr-0012-report.md | a654045 |
| 18-adding-a-game | 3 | done | docs/adding-a-game.md, implementation-artifacts/strategy-discovery-m1-18-adding-a-game-report.md | e80b068 |
| 19-readme-claude | 3 | done | README.md, CLAUDE.md, implementation-artifacts/strategy-discovery-m1-19-readme-claude-report.md | 1f05e04 |
| 20-gate | 3 | done | implementation-artifacts/strategy-discovery-m1-20-gate-report.md | d38ba45 |
| 21-determinism | 4 | done | artifacts/reproducibility/tournament-determinism.md, implementation-artifacts/strategy-discovery-m1-21-determinism-report.md | e53c65d |
| 22-throughput | 4 | done | artifacts/benchmarks/tournament-throughput.md, artifacts/benchmarks/tournament-throughput.json, implementation-artifacts/strategy-discovery-m1-22-throughput-report.md | a9d3b9c | |
| 23-gate-c-record | 4 | done | docs/component-evaluation.md, implementation-artifacts/strategy-discovery-m1-23-gate-c-record-report.md | d3e0522 |
| 24-gate | 4 | done | implementation-artifacts/strategy-discovery-m1-gate-report.md, implementation-artifacts/strategy-discovery-m1-24-gate-report.md | 05cedb5 |

Phase 1 notes (decomposed 2026-08-22 at `f9b829f`; revised 2026-08-22 at `a7ba06f` after the `float_roundtrip` amendment; every API fact quoted into a step was read from the live source at that commit, and every grep/sed acceptance pattern was dry-run against it):

- Dependency graph / waves (as revised): wave 1 = {01-dev-deps, 03-schema-records, 04-bundle-roster, 05-logging-fix} in parallel — pairwise disjoint scopes (`Cargo.toml`+`Cargo.lock` / `src/io/{schema,mod}.rs` / `src/discovery/bundle.rs`+`src/games/tictactoe/corpus.rs` / `src/cli/logging.rs`) — DONE. Wave 2 = 02-invariants (depends on 01; touched `tests/invariants.rs` + `tests/invariants.proptest-regressions`) — ran, commit 41814c7 is on the branch, 3/7 tests fail pending wave 3. Wave 3 = 07-float-roundtrip alone (depends on 01 because it edits `Cargo.toml` after 01 did; its base assertion also checks `tests/invariants.rs` and the regressions file from 41814c7, which its acceptance exercises: `cargo test --test invariants` → `7 passed`). Wave 4 = supervisor re-verification of 02-invariants against the integrated tree after 07 merges — NO worker run: `tests/invariants.rs` at 41814c7 is faithful to the step and to brief D8 and needs no edit; run 02's existing acceptance block verbatim (all nine checks, including `ls proptest-regressions 2>/dev/null | wc -l` → `0`, which still holds because proptest persisted to `tests/invariants.proptest-regressions`, not a top-level directory) and flip 02 to `done` at 41814c7. Wave 5 = 06-gate (report only; depends on all six; its first action asserts one concrete artifact of each; 15 DoD items).
- 07-float-roundtrip scope vs 02: 07 must not touch `tests/invariants.rs` or `tests/invariants.proptest-regressions` (acceptance: `git log --oneline -- tests/invariants.rs tests/invariants.proptest-regressions | wc -l` → `1`). The regressions file (three persisted seeds from the original failure) is KEPT as a committed regression guard per brief D8 ("if one appears from a real failure, commit it"); it was added to 02's `files_in_scope` so the step record matches commit 41814c7. proptest replays those seeds first on every run.
- Dry run at `a7ba06f` (decomposer, in-tree, then reverted): with the one-line feature change applied, `cargo test --test invariants` → `test result: ok. 7 passed; 0 failed` (finished in 0.04 s) and `Cargo.lock` was NOT rewritten (feature-only change); `git diff dcd22b1 -- Cargo.toml | grep -E '^[-+]' | grep -vE '^(\+\+\+|---)'` → exactly `-serde_json = "1.0.151"`, `+serde_json = { version = "1.0.151", features = ["float_roundtrip"] }`, `+`, `+[dev-dependencies]`, `+assert_cmd = "2.2.2"`, `+proptest = "1.11.0"`, `+tempfile = "3.27.0"` — the gate's item 8 expects these seven lines verbatim.
- Couplings: none at content level. 03 records no facts about files 04/05 edit; 04 adds a field to `GameBundle` whose only struct-literal constructor is `src/games/tictactoe/corpus.rs` (grep-verified: no other `GameBundle {` literal in `src/` or `tests/`), so no sibling step can break on the new field. 02's generators use only pre-existing API (`TicTacToeRules`, `TicTacToeCanonicalizer`, `FeatureExpr`, `HeuristicStrategy`, `StrategySpec`), none of which Phase 1 changes.
- Network: 01 is the only step that fetches crates (`cargo add --dev proptest tempfile assert_cmd`); the registry cache already holds `proptest-1.11.0` and `tempfile-3.23.0` but NOT `assert_cmd`. Workers for 02 and later hit the cache. A fresh worktree after 01 compiles proptest/assert_cmd on its first `cargo test --no-run` (expect ~30-60 s instead of the ~10 s baseline).
- Baseline facts for the gate: `cargo test --lib 2>&1 | grep -c " INFO "` = 38 at `f9b829f` (must be 0 after 05); `cargo test --lib tictactoe::corpus` = `4 passed` (5 after 04); `cargo test --workspace` = 18 `test result: ok` lines at baseline and must show 19 after Phase 1 (lib + bin + 16 integration binaries incl. `invariants` + doctests). At `a7ba06f` it shows only 12 `ok` + 1 `FAILED` because cargo stops at the first failing binary (`invariants`) — not a regression; 19 returns once 07 lands. Wall 34.4 s, budget 40 s — `invariants` runs in ~0.04 s (256 cases for state invariants, 64 for serde; `prop_recursive(4, 64, 8, ..)`); `float_roundtrip` slows only float parsing, so the budget is not at risk.
- Emergent contracts Phase 2/3 must honor (all pinned verbatim in step 03 and re-exported from both `crate::io` and `crate::io::schema`): consts `EVALUATION_FILE = "evaluation.json"`, `ARCHIVE_FILE = "archive.json"`, `ARCHIVE_ENTRIES_FILE = "entries.jsonl"` (+ `ANALYZE_FILE` now re-exported); `Tally { games, wins, draws, losses, unfinished, win_rate, draw_rate, loss_rate }` with `Tally::from_counts(wins, draws, losses, unfinished)` (games = sum; rates `count / games`, `0.0` when `games == 0`) — Phase 2's tournament must build every tally through it; `PairingResult { opponent, seat, player, #[serde(flatten)] tally: Tally }` and `OpponentResult { opponent, #[serde(flatten)] tally: Tally, by_seat }` — the counts are INLINED in JSON (document keys `games`, `wins`, ... at the pairing/opponent level, as the brief's D3 field lists require) but accessed in Rust as `.tally.games`; `TournamentResult { roster_id, reference, games_per_pairing, seed, opponents, totals: Tally }`; `AgreementSummary { positions, agreeing, rate, by_value: BTreeMap<i8, AgreementCounts> }` (`AgreementCounts` stays in `crate::discovery::agreement`; `i8` keys serialize as the JSON strings `"-1"`, `"0"`, `"1"`); `BehaviorSignature { sample_id, actions: Vec<serde_json::Value> }`; `Headline`, `StrategyEvaluation`, `AnnotationSource { mode, run_id, records }`, `EvaluationConfig`, `EvaluationReport`, `ArchiveIndexEntry`, `ArchiveIndex`, `Provenance`, `Novelty`, `ArchiveEntry` exactly per brief D3-D6. The hash-key structs (`EvaluationKey`, `SampleKey`, `EntryKey`) are NOT Phase 1 types: Phase 2 defines them privately in `evaluate.rs` / `archive.rs`. `TournamentConfig` (a runtime input, not a record) is also Phase 2's (`tournament.rs`).
- `GameBundle.roster: Roster` sits directly after `default_strategies`; tic-tac-toe sets `roster = benchmark_roster()` and `default_strategies = roster.entries.clone()`. Phase 2's harness reads opponents from `bundle.roster` unless given an explicit `Roster`.
- Fact for the Phase 3 decomposer (verified in `src/core/features.rs` at `f9b829f`): `FeatureVocabulary` is a plain struct `{ defs: Vec<FeatureDef> }` with derived serde (NOT `#[serde(transparent)]` — that attribute is on `FeatureVector`), so an empty vocabulary serializes as JSON `{"defs":[]}` and in TOML as a sub-table `[strategies.spec.heuristic.definitions]` + `defs = []`. Brief D10's sketch `definitions = []` would not parse; the brief itself says to verify with a unit round-trip before pinning `tests/fixtures/strategies-heuristic.toml`, so no amendment was raised — the Phase 3 step must pin the `defs = []` form (or build the fixture text from `toml::to_string(&StrategyFile {..})` in a unit test).
- `proptest-regressions` files are not gitignored by decision (brief): a real property failure commits its regression file. One exists: `tests/invariants.proptest-regressions` (three seeds, committed in 41814c7), kept permanently as the regression guard for ADR 0013.
- Emergent contract from the revision: `Cargo.toml` `[dependencies]` carries exactly one change versus `dcd22b1` (`serde_json = { version = "1.0.151", features = ["float_roundtrip"] }`); Phases 2-4 must not add dependencies or features, and the project gate (Phase 4) re-checks project DoD items 9 (ADR 0013 exists) and 11 (the exact diff shape) — its Cargo.toml expectation is the seven-line diff listed above.

Phase 2 notes (decomposed 2026-08-22 at `3863585`; every API fact quoted into a step was read from the live source at that commit, and every pinned number below was measured by an in-tree dry run at that commit, then reverted):

- Dependency graph / waves: wave 1 = 08-scaffold alone (declares `pub mod archive; pub mod evaluate; pub mod tournament;` with doc-only stub files + `tests/fixtures/roster-tiny.toml`; no re-exports because the modules are empty). Wave 2 = {09-tournament, 10-archive} in parallel (scopes `src/discovery/tournament.rs` / `src/discovery/archive.rs`, disjoint; each replaces its stub). Wave 3 = 11-evaluate alone (depends on 09 for `run_tournament` and on 10 because it also adds the `src/discovery/mod.rs` `pub use` lines for all three modules). Wave 4 = 12-tournament-bench (depends on 11; uses the re-exports). Wave 5 = 13-gate (report only; 14 DoD items; base assertions on one artifact of each of 08-12).
- `src/discovery/mod.rs` is edited by TWO serialized steps (08: `pub mod` lines; 11: `pub use` lines), never by co-parallel ones — a deliberate deviation from the planner note "one Phase 2 step edits mod.rs": the modules must be declared before 09/10 can compile in their worktrees, while re-exports can only be added once the names exist. `pub use` line count goes 10 → 13 (item 11 of the gate).
- Couplings: 09 and 10 record no facts about each other's files. 11 quotes 09's signatures (`run_tournament`, `TournamentConfig`) and 10's public names (for the re-exports) — both are fixed verbatim in the 09/10 step files, so the merged tree matches as long as the workers keep the pinned signatures (each step's acceptance greps them). 12 quotes 11's signatures the same way.
- Pinned numbers workers must reproduce (seed wiring checks; measured at `3863585` with the exact pairing/seed rules in the steps): `random` under test vs `tests/fixtures/roster-tiny.toml` at `seed = 20260822`, `games_per_pairing = 2` → against `perfect`: games 4, wins 0, draws 1, losses 3 (`loss_rate 0.75`); `perfect` under test loses 0 of 12. Agreement over the `generate-small` corpus annotations (run_id `5eafa65f76637df3`, 390 records, ALL non-terminal — corpus positions are pre-move states — `by_value` positions `-1: 48, 0: 200, 1: 142`): `perfect` 390/390; `random` at `seed = 0` with `provider.create(game_seed(splitmix64(seed ^ 0x9E3779B97F4A7C15), index))` → exactly 223/390; signature `stride = 1`, 256 actions, `sample_id = "07a51aa7499cf679"` (= `config_hash` of `{"mode":"corpus","run_id":"5eafa65f76637df3","nonterminal":390,"stride":1}`). For the exhaustive set (4520 non-terminal of 5478, stride 17) the sample_id would be `da984a1596e6d991` — Phase 3/4 may use it as a cross-check.
- Budget facts (debug, warm): one depth-9 game ≈ 50 ms serial; `perfect` vs tiny roster 12 games ≈ 0.6 s; agreement of `perfect` over 390 records ≈ 0.09 s on rayon (0.41 s serial); generate + annotate of `generate-small` ≈ 0.4 s. Expected additions: lib unit tests ≈ 3-4 s of CPU spread over the lib binary's parallel test threads; `tournament_bench` ≈ 2 s (1 game/pairing × 14 pairings × 5 modes + the agreement bench; its acceptance caps `finished in` at < 4.0 s). `cargo test --workspace` should land ≈ 36-40 s against the 50 s budget; `test result: ok` lines 19 → 20.
- Emergent contracts Phase 3 must honor (all pinned verbatim in steps 09/10/11 and re-exported from `crate::discovery`): `TournamentConfig { games_per_pairing, seed, max_plies, reference, threads, serial }`, `TournamentRun { result, plies }`, `run_tournament`, `run_tournament_detailed`, `default_reference(&Roster) -> Result<String, CorpusError>` (LAST entry; the CLI's `--reference` default); `EvaluateConfig { evaluator, games_per_pairing, seed, max_plies, reference, threads, serial }`, `AnnotationSet<G> { metadata: AnnotateMetadata, records }`, `load_annotations(bundle, dir)` (Precondition error text `"{path} not found; run `annotate --exhaustive --game {game} --out {dir}` or `annotate --corpus {dir}` first"`), `measure_agreement(bundle, &dyn StrategyProvider<G>, &AnnotationSet<G>, seed) -> AgreementOutcome { summary, signature }`, `evaluate_strategies(bundle, &[RosterEntry], Option<&Roster>, Option<&AnnotationSet<G>>, &EvaluateConfig) -> EvaluationReport` (validates the roster, builds every provider up front, strategies-file order), `evaluate_strategy`, `headline`, `evaluation_id`, `mode_name(AnnotateMode) -> "corpus" | "exhaustive"`, `strict_failures(&EvaluationReport, max_loss_rate) -> Vec<String>` (each `"reference loss rate exceeded: {name} {:.3} > {:.3}"`; the CLI joins with `"; "` for the `CliError::CheckFailed` message); `Archive::open(dir, game)` (creates dir + empty `archive.json` + empty `entries.jsonl`; other game → `CorpusError::Config` containing "belongs to game"), `Archive::append(NewEntry { name, spec, provenance, evaluation }) -> Appended::{New(id), Duplicate(id)}` (Duplicate writes nothing), `entries()`, `len()`, `get(id)`, `index()`, `entry_id(game, spec, evaluation_id)`, `novelty`, `spec_distance`, `behavior_distance`, `NOVELTY_METHOD = "m1-v1"`. Novelty semantics fixed in step 10: per-entry `d_e = behavior_distance.unwrap_or(spec_distance)`, nearest = argmin (first on ties), `distance = min behavior_distance (comparable entries) else min spec_distance`, `is_novel = distance > 0.0`. Private hash keys (field order is the hash): `EvaluationKey { game, roster, config, strategies }`, `SampleKey { mode, run_id, nonterminal, stride }`, `EntryKey { game, spec, evaluation_id }`.
- Unit tests in the three modules write only under `target/` via `tempfile::Builder::new().prefix(..).tempdir_in(concat!(env!("CARGO_MANIFEST_DIR"), "/target"))` (lib unit tests have no `CARGO_TARGET_TMPDIR`). The game-name awk rule is asserted per new file in 09/10/11 and re-checked by the gate (item 12).
- Fact for Phase 3 (heuristic fixture, verified at `3863585`): `toml::to_string(&StrategySpec::HeuristicRules { heuristic: HeuristicStrategy::new("empty") })` is `kind = "heuristic-rules"` / `[heuristic]` `name = "empty"` `kind = "heuristic-rules"` `rules = []` / `[heuristic.definitions]` `defs = []` / `[heuristic.fallback]` `select = "any-legal"` — inside a `StrategyFile` the tables become `[strategies.spec]`, `[strategies.spec.heuristic]`, `[strategies.spec.heuristic.definitions]`, `[strategies.spec.heuristic.fallback]`. The harness surfaces such an entry as `CorpusError::Match(MatchError::Strategy(StrategyError::Unimplemented{..}))` whose Display is `strategy `heuristic-rules` is not implemented: rule interpreter execution lands with heuristic mining (plan.md Phase 8)` (exit 2 via the existing mapping).

Phase 3 notes (decomposed 2026-08-22 at `c2d764f`; the decomposer implemented the `evaluate` CLI, the two fixtures and `tests/cli_evaluate.rs` in-tree as a dry run, measured every number below, verified all five quality gates, then reverted — the step files carry that code verbatim, so steps 14 and 15 are transcription, not design):

- Dependency graph / waves: wave 1 = {14-evaluate-cli, 16-adr-0011, 17-adr-0012, 18-adding-a-game} in parallel — pairwise disjoint scopes (`src/cli/{evaluate,mod}.rs` + two fixtures / `docs/adr/0011-*` / `docs/adr/0012-*` / `docs/adding-a-game.md`). Wave 2 = {15-cli-evaluate-tests (depends on 14: it runs the binary), 19-readme-claude (depends on 14 for the live stdout it copies, and on 16/17/18 so the README links it adds resolve)} in parallel — scopes `tests/cli_evaluate.rs` / `README.md` + `CLAUDE.md`. Wave 3 = 20-gate (report only; 16 DoD items; base assertions on one artifact of each of 14-19).
- `src/cli/mod.rs` is edited by exactly one step (14): `mod evaluate;`, the `Evaluate(evaluate::EvaluateArgs)` variant, the dispatch arm, and two module-doc lines. `src/cli/games.rs` and `src/cli/error.rs` are untouched (no new error variant: `CliError::CheckFailed` → 3 and the existing `Unimplemented`/`Config`/`Precondition`/NotFound → 2 mappings cover every D7 case; `--threads 0` is rejected as `CorpusError::Config` like `generate` does).
- Implementation note (deliberate, DoD-neutral, for the lead): the CLI calls the library's `evaluate_strategies` (which has owned the per-strategy loop and the fail-fast provider build since Phase 2) and wraps its error with the anyhow context `evaluating strategies from {strategies-file path}`. So a play-time failure reads `error: evaluating strategies from tests/fixtures/strategies-heuristic.toml: strategy `heuristic-rules` is not implemented: rule interpreter execution lands with heuristic mining (plan.md Phase 8)` — it names the strategies FILE, not the entry (brief D7's parenthetical says "the `anyhow` context names the entry: `evaluating strategy `{name}``"). Naming the entry would mean duplicating the library loop and the report assembly in the CLI (and losing the unit test that pins it); the DoD contract (exit 2, stderr contains `heuristic-rules` and `not implemented`, no `evaluation.json`) is met exactly. If the entry name is wanted, the clean fix is a library change (`evaluate_strategies` wrapping per-entry errors), i.e. a Milestone 2 item or a brief amendment — not a Phase 3 step.
- Pinned numbers workers must reproduce (`--roster tests/fixtures/roster-tiny.toml --games 2`, default seed 0, `generate-small` corpus annotations, fixtures exactly as in step 14): stdout `strategy=perfect kind=minimax games=12 wins=5 draws=7 losses=0 unfinished=0 loss_rate_vs_reference=0.000 agreement=1.000 novelty=1.000` / `strategy=random kind=random games=12 wins=1 draws=1 losses=10 unfinished=0 loss_rate_vs_reference=0.750 agreement=0.572 novelty=0.488` / `strategy=depth-3 kind=minimax games=12 wins=3 draws=7 losses=2 unfinished=0 loss_rate_vs_reference=0.000 agreement=0.956 novelty=0.184` / `evaluation=bce1c9d8c566106e game=tictactoe roster=tiny-v1 reference=perfect strategies=3 archive_entries=3 out=<dir>`; without `--annotations`: `agreement=none` ×3, `evaluation=e0368cde740bd9c2 ...`, and with `--strict` exit 3 + `error: check failed: reference loss rate exceeded: random 0.750 > 0.000`. Full scale (project DoD item 3: `annotate --exhaustive` 5478 records, then `evaluate --strategies tests/fixtures/strategies-eval.toml --annotations <exhaustive> --out <dir>`, default `--games 20`, roster `ttt-benchmark-v1`): `strategy=perfect ... games=280 wins=87 draws=193 losses=0 unfinished=0 loss_rate_vs_reference=0.000 agreement=1.000 novelty=1.000` / `strategy=random ... games=280 wins=25 draws=26 losses=229 unfinished=0 loss_rate_vs_reference=0.875 agreement=0.579 novelty=0.535` / `strategy=depth-3 ... games=280 wins=87 draws=146 losses=47 unfinished=0 loss_rate_vs_reference=0.150 agreement=0.978 novelty=0.051` / `evaluation=44ad0fffbef146da game=tictactoe roster=ttt-benchmark-v1 reference=perfect strategies=3 archive_entries=3 out=<dir>` — Phase 4's Gate C evidence can cross-check against these. Byte-identity verified in the dry run: repeat run (fresh dir), same-dir rerun (no new entries), `--serial`, `--threads 2`, `-v` → identical `evaluation.json`/`archive.json`/`entries.jsonl` SHA-256s and identical stdout up to the `out=` echo.
- Budget facts (debug): one test-scale `evaluate` run ≈ 0.7 s; the full-scale run ≈ 3.0 s (+ 0.6 s exhaustive annotation); `tests/cli_evaluate.rs` (4 tests, 9 binary runs) finishes in ≈ 5.3 s; warm `cargo test --workspace` wall 43.1 s with 21 `test result: ok` lines (20 before step 15 lands) against the 60 s budget. `cargo run -q` propagates the binary's exit code and adds nothing to stderr (verified) — the acceptance blocks rely on it.
- Fixture contract (verified by the dry run): `tests/fixtures/strategies-heuristic.toml` uses `[strategies.spec.heuristic.definitions]` + `defs = []` (the brief's `definitions = []` sketch does not parse); it parses, builds, and fails at play time with `Unimplemented` (exit 2). `tests/fixtures/strategies-eval.toml` = `perfect` (depth 9), `random`, `depth-3` (depth 3), in that order (the order is part of `evaluation_id`).
- Couplings: 15 quotes nothing volatile from 14 beyond the frozen stdout contract and fixture names; 19 copies its stdout block from its own live run and only asserts the decomposer-measured lines as a cross-check; 19's README links name files 16/17/18 create (hence the dependency). 16 and 17 refer to each other only by ADR number. No step records line numbers or counts of a sibling's file.
- Environment facts for Phase 4: `runs/` is NOT gitignored (step 19 runs its live command into `runs/` to match the README's path conventions and deletes it afterwards; Phase 4 scripts must keep scratch under `target/m1/`); the README stdout block ends `out=runs/eval`, so any cross-check against a live run must normalize the `out=` token (the gate does). `docs/adr/` will hold 13 files after Phase 3.
- Emergent contracts Phase 4 must honor: the two stdout line shapes exactly as in step 14 (`novelty` = the archived entry's `novelty.distance`, `archive_entries` = the archive total after the run); `--archive DIR` defaults to `<out>/archive`; `evaluate` writes nothing on any error before the write step and everything (files + archive) before a `--strict` failure; the exit-code table rows 2 and 3 in README now mention `heuristic-rules` and `evaluate --strict`.

Phase 4 notes (decomposed 2026-08-23 at `78a160a`; both evidence scripts were dry-run in-tree by the decomposer against the debug/release binaries built from `78a160a`, every acceptance pattern was dry-run against the resulting files or stub files, and all generated files were removed afterwards — the working tree is untouched apart from the step files and this ledger):

- Dependency graph / waves: wave 1 = 21-determinism alone (debug binary, ≈ 40 s script). Wave 2 = 22-throughput (release binary + release bench test binary, ≈ 80 s script after two ≈ 20 s warm builds; minutes in a fresh worktree). 22 `depends_on` 21 ONLY to keep the two CPU-heavy evidence runs from overlapping — timing fidelity, not a file dependency (its base assertion checks 21's artifact exists; it never reads it). Wave 3 = 23-gate-c-record (depends on 21 and 22: it copies their numbers into the `## Gate C` rows and runs the five quality gates for row 1). Wave 4 = 24-gate (depends on 21, 22, 23; 14 items = the 12 project DoD items + 2 Phase 4 DoD items: md/json number cross-check and the pure-addition diff of `docs/component-evaluation.md`). No step touches source, tests or existing artifacts; all scopes are pairwise disjoint (21: `artifacts/reproducibility/tournament-determinism.md`; 22: `artifacts/benchmarks/tournament-throughput.{md,json}`; 23: `docs/component-evaluation.md`; 24: `implementation-artifacts/strategy-discovery-m1-gate-report.md` + its worker report).
- Run-location advice for the supervisor (roadmap Phase 4 risk): 22 needs `cargo build --release` and `cargo test --release --test tournament_bench --no-run` — in a fresh worktree that is a full release build (minutes). Running 21/22 in the integrated checkout (or a worktree whose `target/` already holds release artifacts) is allowed by the roadmap; a worktree is fine too if the build time is acceptable. Keep the machine otherwise idle while 22's script runs. All scratch lives under `target/m1/` (gitignored); the scripts delete and recreate `target/m1/determinism` and `target/m1/throughput` themselves. `runs/` is NOT gitignored — every step's acceptance checks that `git status` shows nothing under it.
- Scripts are pinned verbatim in the step files (21: `target/m1/tournament-determinism.ps1`, 22: `target/m1/tournament-throughput.ps1`) and must be embedded verbatim in the artifacts' `## Commands` blocks — each acceptance diffs the embedded block against the script that ran (CR-stripped). Workers create the scripts with the Write tool (Bash-tool commands > ~8 KB truncate) starting at column 0.
- Measured by the decomposer's dry runs at `78a160a` (expected magnitudes, not pins): determinism — all five claims `identical: YES`; `evaluation.json` 38924 bytes sha256 `1487b18b…`, `archive.json` 429 bytes, `entries.jsonl` 15448 bytes / 3 lines, identical across run-1, run-2, `--serial`, `--threads 4`, `-v`, and unchanged after the third run into run-1 (`entries.jsonl` lines before=3 after=3); stdout (with the ` out=…` echo removed) identical across all six runs; full-scale stdout exactly the Phase 3 pinned lines (`evaluation=44ad0fffbef146da … archive_entries=3`). Throughput (release, two dry runs) — `tests/tournament_bench.rs`: serial ≈ 267 games/s ≈ 448 us/move; pool ≈ 1510-1550 games/s, speedup ≈ 5.6-5.75; threads 2/4/8 ≈ 500/890/1400 games/s; agreement bench (corpus, 390 positions) ≈ 41-45 k positions/s; CLI `evaluate --games 100` (4200 games): `--serial` ≈ 6.5-6.6 s (≈ 640 games/s), `--threads 1` ≈ 6.6 s, 2 ≈ 3.9 s, 4 ≈ 2.1 s, 8 ≈ 1.3 s, default pool ≈ 1.07-1.09 s (≈ 3850-3900 games/s); agreement over the exhaustive annotation via `--games 0 --annotations` (13560 positions incl. process start + load) ≈ 0.05 s (≈ 2.6e5 positions/s); canonical `--games 100 --annotations` ≈ 1.1-1.13 s. The bench's games/s is lower than the CLI's because `perfect` vs `perfect` (full depth-9 searches every move) dominates the bench while `strategies-eval.toml` also holds the cheap `random` and `depth-3`.
- Design facts fixed in the steps: the determinism script's five verdict lines are the only `identical: YES|NO` text in the artifact besides the closing `all identical: YES|NO` (the script source says `identical: {0}`), so the Phase 4 DoD greps (`≥ 4` YES, `0` NO) are honest. Agreement positions/s over the exhaustive set is measured with `--games 0` (accepted by the CLI: zero games per pairing, all rates 0.0, `evaluation=eef6aef908d58e4c`) because the CLI exposes no separate agreement timing; the bench's corpus-scale agreement figure is reported alongside. The throughput `.json` twin is WRITTEN BY THE SCRIPT (ConvertTo-Json, pretty, eight-field header with `git rev-parse HEAD` and today's date); the `.md` is transcribed from the script's summary lines, whose numbers are formatted (`0.0`, `F3`, `0.00`) to match what `ConvertFrom-Json ... .ToString(...)` yields, so the gate's md/json cross-check (`tournament_bench[0].median_games_per_s` formatted `0.0`, `evaluate_cli[0].median_wall_s` formatted `F3`) is satisfiable without retyping.
- Gate C record (step 23): five rows — plan lines 186-189 (four Phase 7 acceptance checks) + the Gate C line (plan line 275), `Gate C verdict: PASS` as the file's last line, appended after `Gate B verdict: PASS` (pure-addition hunk `@@ -156,0 +157,`; verified on a stub). Row 1 ("pass in CI") honestly cites the local run of the CI command set plus `.github/workflows/ci.yml`, since the branch reaches `develop` by squash merge and CI runs after that. Placeholder-free check: the section must contain no `<` (the row texts were worded to avoid literal angle brackets).
- Project gate (step 24): writes `implementation-artifacts/strategy-discovery-m1-gate-report.md` (the brief's project gate report, project DoD item 12) in addition to its worker report; item 12 counts committed worker reports (`git ls-files` → 23 for steps 01-23) and step files (24, its own included) and RECORDS the ledger `done` count with expectation `≥ 20` rather than asserting 23, because the supervisor flips 21-23 to `done` around the time the gate runs — ledger completeness stays the supervisor's/lead's responsibility. `cargo test --lib archive` matches 7 tests (6 in `discovery::archive` + `io::schema::tests::archive_records_round_trip_and_field_names`). The `$_` inside the pwsh JSONL parse command is written `\$_` in the step because bash expands a bare `$_`.
- Emergent facts for the lead-developer's milestone evaluation: the project DoD item 3 numbers (`perfect` 280 games 87/193/0, `random` 0.875, `depth-3` 0.150 reported-not-asserted, agreement 1.000 / 0.579 / 0.978) reproduced on every dry run at `78a160a`; the `evaluate` archive after the full-scale run holds exactly 3 entries (`59205dd18a2b9992` perfect, `d859aad522920c15` random, `dedf2656f0ec6590` depth-3).

Planner notes for the decomposer:

- Baseline at `48c97c8` (debug): 18 `test result: ok` lines, 360 passed (lib 296), wall 34.4 s; `tests/match_bench.rs` alone is 17.9 s. Per-binary counts, never suite-wide totals, in acceptance blocks.
- `src/io/schema.rs` and `src/io/mod.rs` are edited by ONE Phase 1 step (the record-types step); `src/discovery/mod.rs` by ONE Phase 2 step; `src/cli/mod.rs` by ONE Phase 3 step. `src/cli/games.rs` is never edited in this milestone.
- Phase 1's record-types step is the contract for Phase 2: quote the brief's D3-D6 field lists verbatim into the step and assert JSON field names in its acceptance.
- Phase 3 (b) documentation steps (ADR 0011, ADR 0012, `docs/adding-a-game.md`) have no source dependency beyond Phase 1 and run in the first wave of Phase 3; the README/CLAUDE.md step runs after the command step because its stdout block is copied from a live run.
- Environment: worktrees under `C:/Users/John/Projects/worktrees/`; fresh-worktree debug `cargo test --no-run` ≈ 10 s; one cargo command per worker tool call; no `python`; Bash-tool commands > ~8 KB truncate — workers author files with Write/Edit; `core.autocrlf = true`; `cargo test` stderr carries INFO/TRACE lines from the lib test binary until the Phase 1 logging fix lands.
- Network: the first `cargo add --dev proptest tempfile assert_cmd` fetches crates; schedule it in Phase 1 wave 1 so later workers hit the registry cache.

## Revisions

<!-- supervisor appends: phase | failed step | revision note | outcome -->

| phase | failed step | revision note | outcome |
| --- | --- | --- | --- |
| 1 | 02-invariants | ESCALATED to the user, not sent to the decomposer. `tests/invariants.rs` is faithful to the step; 4/7 tests pass; the 3 serde round-trip tests fail on a real defect the property caught: the crate depends on `serde_json` WITHOUT the `float_roundtrip` feature, so `serde_json::from_str::<f64>(&serde_json::to_string(&v))` is off by one ULP for ~10.4% of f64 values (verified independently by the supervisor in a standalone crate: 20737/200000 failures at 1.0.151 AND at 1.0.145, so it is not the zmij codec; the writer is bit-correct and Rust `str::parse::<f64>` on the same text is correct; with `features = ["float_roundtrip"]` failures drop to 0/200000). Fix = `serde_json = { version = "1.0.151", features = ["float_roundtrip"] }` in `[dependencies]` of `Cargo.toml`. Blocked because step 01 context says "nothing may change under `[dependencies]`" and Phase 1 DoD item 8 requires `git diff dcd22b1 -- Cargo.toml` to show ONLY the three dev-dependencies — jointly unsatisfiable with brief D8. A brief/roadmap amendment (scope/DoD change + a dependency-feature decision with a parse-speed cost) needs user approval; the supervisor must not weaken the property instead. | approved by the user 2026-08-22 ("Approve feature + ADR.") and applied by the planner — see the next row (amendment `permit serde_json float_roundtrip feature and ADR 0013`) |
| 1 | 02-invariants | AMENDMENT (planner, user-approved scope/DoD change). Brief: Constraints `Cargo.toml` bullet and project DoD item 11 now permit exactly one `[dependencies]` change, `serde_json = { version = "1.0.151", features = ["float_roundtrip"] }` (same version, no `preserve_order`, every other prohibition intact); dependency inventory line updated; `Next ADR numbers` line now lists 0013; D11 gains `docs/adr/0013-serde-json-float-roundtrip.md` (Phase 1; format per existing ADRs; Status `Accepted — 2026-08-22 (Milestone 1 / plan Phase 7)`; 20737/200000 versus 0/200000 evidence; parse-speed versus bit-exactness trade); project DoD item 9 requires ADR 0013 to exist; D8 gains an explicit "bit-exact float round trip requires `float_roundtrip`, never relax" bullet (D8 generators, seven test names and round-trip definition UNCHANGED); Assumptions gain "the feature changes only the parser, so pinned run_ids, byte-identity artifacts and budgets are unaffected". Roadmap: intro paragraph, Phase 1 objective (new item (a2)), scope (+ ADR 0013 file), DoD (`git diff dcd22b1 -- Cargo.toml` clause now allows the single `serde_json` line change; ADR 0013 existence/structure/checker clauses added) and risks. ADR numbers 0011/0012 keep their meanings (Phase 3). Phases 2-4 DoDs unchanged; this strengthens the byte-identity and round-trip items they rely on. Pending step 06-gate and re-verification of 02-invariants go through the decomposer's revision operation; the `Cargo.toml` feature line and the ADR file are new execution work, not plan edits. | brief amended |
| 1 | 02-invariants | DECOMPOSER REVISION at `a7ba06f` (2026-08-22). Added step `07-float-roundtrip` (scope `Cargo.toml`, `Cargo.lock`, `docs/adr/0013-serde-json-float-roundtrip.md`, its report; depends_on 01; acceptance proves the fix with `cargo test --test invariants` → `7 passed` and pins the exact one-line `[dependencies]` diff, the ADR structure/checkers and the five quality gates; dry-run in-tree: 7 passed, `Cargo.lock` untouched). `06-gate` rewritten: depends_on += 07; base assertions += `grep -c float_roundtrip Cargo.toml` → 1 and the ADR file; item 8 now expects the seven-line `git diff dcd22b1 -- Cargo.toml` shape (one `-` line) plus `float_roundtrip` and the `game-player` pin checks; new item 15 for ADR 0013; acceptance counts 14 → 15 and the `### Item` range widened to `1[0-5]`. `02-invariants`: kept, not re-run — `files_in_scope` += `tests/invariants.proptest-regressions` (matches commit 41814c7; file kept as a regression guard per D8); status `reverify`: the supervisor re-runs 02's existing acceptance block on the integrated tree after 07 merges and flips it to `done`. Steps 01, 03, 04, 05 untouched. | steps revised; 07 pending, 06 pending, 02 reverify |
| 1 | 02-invariants | Resolution of the escalation above. Amendment landed at `a7ba06f`; decomposer revision at `e47fae1` added `07-float-roundtrip` (Cargo.toml `float_roundtrip` + ADR 0013) and revised `06-gate` (14 -> 15 items, item 8 rewritten, ADR 0013 item added, `depends_on` += 07). Step 07 merged at `13418ba`; the supervisor then re-ran step 02's acceptance block verbatim on the integrated tree — `cargo test --test invariants` = `ok. 7 passed; 0 failed`, all seven named tests, the three persisted regression seeds replayed and passed — so 02 was flipped to `done` at its original commit `41814c7` with no worker re-run and no change to `tests/invariants.rs`. | resolved — 02 done, 07 done |
| 1 | 06-gate | Corrected in flight (no decomposer round-trip): the step ran honestly and returned `missing-base` at base assertion 5, `sed -n ... | head -3 | grep -c "init(0, true);"` → 0. The assertion was MIS-SPECIFIED, not the tree: step 05 added a two-line explanatory comment between the `fn` signature and the call, pushing `init(0, true);` to the 4th line of the sed range and outside the `head -3` window. The reorder itself is present and `cargo test --lib 2>&1 | grep -c " INFO "` is 0. Assertion replaced with `sed -n ... | grep "init(" | head -1 | grep -c "init(0, true);"` → 1, which asserts the real invariant (the first `init(` call is the quiet one) and tolerates comment lines; dry-run verified → 1. Failed gate commit `9bab727` reverted at `617d82e`; step re-run against the corrected packet. | corrected step file committed; step 06 re-run |
| 4 | 22-throughput | Corrected in flight (no decomposer round-trip); the step PASSED, this records the spec fix. Action 7 specified the `## Evaluate wall` table header as `| command | run1_wall_s | ... |`, which collides with the acceptance regex of the step itself `grep -cE "^\| (date|git_commit|rustc|os|cpu|ram|build_profile|command) \| " -> 8` (that header row is a ninth match) and with the identical regex in step 24 item 10. The worker resolved it honestly by naming the column `cmd` (row content unchanged, no data altered) and logged it as its single deviation; the supervisor verified all 16 acceptance checks and cross-checked every `.md` number against the `.json` (five bench rows, six CLI rows, agreement row, evaluate-wall row — all exact). Step file action 7 amended to specify `cmd` with the reason, so the artifact history matches what ran. No change needed in step 24: its item 10 regex now yields 8 for both evidence files. | step file corrected and committed; step 22 done at a9d3b9c |
