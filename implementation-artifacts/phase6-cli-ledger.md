# phase6-cli — Ledger

Single source of truth for execution state. Sections are owned by different skills —
the planner seeds Plan + Phases; the decomposer fills Steps per phase; the supervisor
updates Steps and appends Revisions.

## Plan

- plan-name: phase6-cli
- current-phase: complete (phases 1-3 all done; plan finished 2026-08-22 at `96cf805`)
- working-branch: feature/phase-6-cli
- starting-commit: f0b633fe6093a76b7d7d92c3965caed800b1f60b
- default-branch: develop
- artifacts-dir: implementation-artifacts/

## Phases

| Phase | Status  | Notes |
| ----: | ------- | ----- |
| 1     | complete | Contracts and foundations: cli split + games.rs dispatch, analyzer registry + summary analyzer + analyze.json, experiment config types + StrategyFile, per-cell helper extraction, logging, exit codes (strict → 3) |
| 2     | complete | Commands: play, analyze (--analyzers/--list-analyzers + agreement), report, pipeline; integration tests |
| 3     | complete | README Usage rewrite, ADR 0010, CLAUDE.md commands line, `artifacts/reproducibility/cli-outputs-determinism.md`, project gate report `Overall verdict: PASS` |

## Steps

<!-- decomposer fills per phase: id | phase | status | files | commit,
     plus a "Phase <N> notes" block: dependency graph, couplings, emergent contracts -->

| id | phase | status | files | commit |
| --- | --- | --- | --- | --- |
| 01-cli-split | 1 | done | src/cli/mod.rs, src/cli/games.rs, src/cli/generate.rs, src/cli/annotate.rs, src/cli/analyze.rs, implementation-artifacts/phase6-cli-01-cli-split-report.md | 1288fa2 (merge 6ae05ab) |
| 02-analyze-registry | 1 | done | src/discovery/analyze.rs, src/discovery/summary.rs, src/discovery/mod.rs, src/discovery/config.rs, src/io/schema.rs, implementation-artifacts/phase6-cli-02-analyze-registry-report.md | 1a41b15 (merge f9293a4) |
| 03-corpus-helpers | 1 | done | src/discovery/corpus.rs, implementation-artifacts/phase6-cli-03-corpus-helpers-report.md | badb957 (merge 41b85f2) |
| 04-logging | 1 | done | Cargo.toml, Cargo.lock, src/cli/logging.rs, src/cli/mod.rs, implementation-artifacts/phase6-cli-04-logging-report.md | c71b90c (merge c7bfedd) |
| 05-experiment | 1 | done | src/discovery/experiment.rs, src/discovery/mod.rs, configs/tictactoe-experiment.toml, tests/fixtures/experiment-small.toml, implementation-artifacts/phase6-cli-05-experiment-report.md | 66129d3 (merge 523d7cc) |
| 06-instrumentation | 1 | done | src/discovery/corpus.rs, src/discovery/annotate.rs, src/discovery/analyze.rs, src/strategy/minimax.rs, tests/cli_logging.rs, implementation-artifacts/phase6-cli-06-instrumentation-report.md | f9a815e (merge 91d529f) |
| 07-exit-codes | 1 | done | src/cli/error.rs, src/cli/mod.rs, src/cli/annotate.rs, src/cli/analyze.rs, src/main.rs, tests/cli_pipeline.rs, implementation-artifacts/phase6-cli-07-exit-codes-report.md | 9e76968 (merge 86b5529) |
| 08-gate | 1 | superseded | implementation-artifacts/phase6-cli-08-gate-report.md | b2abe7f (in-tree, no merge) |
| 09-doc-game-neutral | 1 | done | src/discovery/experiment.rs, implementation-artifacts/phase6-cli-09-doc-game-neutral-report.md | df836e9 (in-tree, no merge) |
| 10-gate | 1 | done | implementation-artifacts/phase6-cli-10-gate-report.md | c8707ba (in-tree, no merge) |
| 11-cli-surface | 2 | done | src/cli/mod.rs, src/cli/analyze.rs, src/cli/play.rs, src/cli/report.rs, src/cli/pipeline.rs, tests/cli_analyze.rs, implementation-artifacts/phase6-cli-11-cli-surface-report.md | 5bf2018 (merge 8258f0e) |
| 12-agreement | 2 | done | src/discovery/agreement.rs, src/discovery/analyze.rs, src/discovery/mod.rs, implementation-artifacts/phase6-cli-12-agreement-report.md | 66a9dd0 (merge f5044da) |
| 13-play | 2 | done | src/cli/play.rs, tests/cli_play.rs, implementation-artifacts/phase6-cli-13-play-report.md | 8239348 (merge 41a060f) |
| 14-report | 2 | done | src/cli/report.rs, tests/cli_report.rs, tests/cli_agreement.rs, implementation-artifacts/phase6-cli-14-report-report.md | eb1f5c4 (merge 06d8e19) |
| 15-pipeline | 2 | done | src/cli/pipeline.rs, tests/cli_experiment.rs, implementation-artifacts/phase6-cli-15-pipeline-report.md | 84ccaa4 (in-tree, no merge) |
| 16-gate | 2 | done | implementation-artifacts/phase6-cli-16-gate-report.md | 589eb9e (in-tree, no merge) |
| 17-readme | 3 | done | README.md, implementation-artifacts/phase6-cli-17-readme-report.md | b4ceefa (merge 974f25b) |
| 18-adr | 3 | done | docs/adr/0010-cli-contract-and-analyzer-registry.md, CLAUDE.md, implementation-artifacts/phase6-cli-18-adr-report.md | 8ca466e (merge 86a4912) |
| 19-determinism | 3 | done | artifacts/reproducibility/cli-outputs-determinism.md, implementation-artifacts/phase6-cli-19-determinism-report.md | 3592722 (merge 5685738) |
| 20-gate | 3 | done | implementation-artifacts/phase6-cli-gate-report.md, implementation-artifacts/phase6-cli-20-gate-report.md | 96cf805 (in-tree, no merge) |

Phase 3 supervisor notes (executed 2026-08-22; wave 1 = {17, 18, 19} in worktrees off `bb25b90`, wave 2 = 20-gate in-tree):

- All four steps passed on the first attempt; no retries, no revisions, no amendments. Worker deviations: none in 17/18/20; step 19 replaced EVERY literal `|` in the `## Runs` stdout cells with `;` (not only the script's `' | '` join separator) because `report`'s stdout is itself a Markdown table full of bare pipes — a stricter reading of the step's own "`| `-free text" rule, accepted.
- Supervisor re-verification on the integrated tree at `96cf805` (not taken from any report): `cargo test --workspace` exit 0, 18 `test result: ok`, 0 FAILED, 360 passed, wall 33 s (budget 45 s); `cargo fmt --all --check`, `cargo clippy --workspace --all-targets --all-features -- -D warnings` (0 warnings) and `cargo doc --no-deps --workspace` (0 warnings) all exit 0. G9 loop: no `VIOLATION`; `src/main.rs` 11 lines, 0 `Command::`. G10 `git diff f0b633f --stat` on plan/component-eval/pre-existing artifacts: empty; the only files changed under `docs`/`artifacts`/`README.md`/`CLAUDE.md` since `f0b633f` are `README.md`, `CLAUDE.md`, `docs/adr/0010-*.md`, `artifacts/reproducibility/cli-outputs-determinism.md`. G5/G6/G7 re-run live: `--list-analyzers` two TAB lines; exit codes 2/2/2/3 with the exact strict stderr line; `-v` stdout SHA-256 identical to plain, verbose stderr 1 `INFO`, plain 0, `RUST_LOG=off` stderr 0 bytes.
- Determinism evidence independently re-checked, not trusted from the file: the supervisor recomputed `sha256sum` over step 19's scratch `target/phase6/determinism/` in its worktree — `play-1` == `play-2` == `gen-equiv` on `run.json`/`games.jsonl`/`positions.jsonl`, `pipe-1` == `pipe-2` on `report.md`/`analyze.json`/`summary.json`/`agreement.json`, and all six pinned Phase 5 hashes reproduce byte for byte. The embedded PowerShell block was diffed against the step spec: byte-identical, 116 lines.
- `README.md` contains no `exit 2`/`exits 2` prose at all (the exit-code table row `| 2 | usage |` is the only place the code appears), which satisfies the roadmap's "refers only to usage errors" item trivially.
- Phase 3 committed no source change, as planned; the `cargo doc` escape hatch in the roadmap scope was never needed.

Phase 3 notes (decomposed 2026-08-22 at `4be036a`; every command output quoted into a step file, the whole determinism PowerShell script, and every acceptance grep/awk below were dry-run against the live tree at that commit):

- Dependency graph / waves: wave 1 = {17-readme, 18-adr, 19-determinism} in parallel — pairwise disjoint scopes (`README.md` / `docs/adr/0010-*.md` + `CLAUDE.md` / `artifacts/reproducibility/cli-outputs-determinism.md`), and none of them touches source, so none can stale another's recorded facts. Wave 2 = 20-gate alone (report-only, depends on all three; its first action asserts one concrete artifact of each).
- No source changes in Phase 3. `cargo doc --no-deps --workspace` is already warning-free at `4be036a`, so the roadmap's "doc-comment fixes surfaced by `cargo doc`" escape hatch is not needed; if a worker thinks it is, that is a signal to stop, not to edit `src/**`.
- 20-gate writes TWO files: the project gate report `phase6-cli-gate-report.md` (the DoD table, the deliverable named by brief DoD item 11) and its own worker report `phase6-cli-20-gate-report.md`. The gate's acceptance greps target the gate report only, so the usual self-reference trap (a `grep -c "Overall verdict:"` that its own acceptance block re-satisfies) cannot fire: the acceptance text lives in a different file from the one being grepped. The worker is explicitly told not to paste the table or the verdict line into its own report.
- Facts captured from the live binary at `4be036a` and embedded verbatim in step 17 (README must not retype them): `play --game tictactoe --players random,perfect --games 2 --seed 7` prints game `0:0` `seed=0x9d20c9ebd5dcd11a` (Draw, length 9) and `0:1` `seed=0xd4c1951159fc36d5` (`Win(O)`, length 6), totals `games=2 wins=X:0,O:1 draws=1 unfinished=0`; `analyze --list-analyzers` prints exactly two TAB-separated lines (`agreement`, `summary`) — the TAB is literal and must survive the edit; `pipeline` on `experiment-small` prints five lines ending `pipeline=experiment-small out=<dir> stages=generate,annotate,analyze,report`.
- Determinism script (step 19) was authored and run here, not by the worker: all six verdicts `identical: YES` and all six pinned Phase 5 SHA-256s reproduce from the DEBUG binary. Deliberate choices baked into it: debug profile (release adds minutes of build for byte-identical output); `Set-Location $Root` plus relative `target/phase6/determinism` paths, so no `C:\...` path can reach the committed evidence; verdict lines worded `..., identical: YES` (ASCII comma, no em dash — an em dash risked console-encoding mojibake) so the acceptance can count them with a single grep.
- Known-correct differences the gate and the evidence file must NOT "fix": `small-*/summary.json`, `analyze.json` and `report.md` differ from `pipe-*/` counterparts (3227 vs 3195 bytes for `report.md`) because `tests/fixtures/experiment-small.toml` sets thresholds 0.25/0.2/0.5 while a standalone `analyze` uses the defaults 0.5/0.2/0.5, hence `diversity_pass=true` from `pipeline` and `false` from `analyze` on the same corpus. Determinism is claimed only between two runs of the SAME command (plus `play --out` vs `generate` on the equivalent sweep).
- New pins usable by later phases: `play`'s equivalent sweep `run_id=988795b19ac38e3c cells=1 games=2 positions=15`, with `run.json` `25492c4b…`, `games.jsonl` `8cf2cf01…`, `positions.jsonl` `ef155fce…`; `generate-small` + summary/agreement chain gives `agreement.json` `556823fd…` (1555 B), `analyze.json` `7927c91a…` (413 B), `report.md` `314a13bb…` (3227 B); the pipeline variant gives `analyze.json` `4bfdc389…`, `summary.json` `2f3fbdd2…`, `report.md` `a9fab4f8…` (3195 B).
- Markdown checkers used in every Phase 3 acceptance, both dry-run clean against the current `README.md`, `CLAUDE.md`, `docs/adr/0009-*.md` and `artifacts/reproducibility/corpus-determinism.md`: `awk '/^```/{f=!f; next} f{next} /^#{1,6} /{h=NR; next} {if(h==NR-1 && $0!=""){v++}} END{print v+0}' FILE` → `0` (blank line after every heading, fence-aware) and `grep -c -- '|---' FILE` → `0`.
- Gate-check dry-runs at `4be036a` (so the gate's expectations are known-satisfiable): the G9 one-game-name loop reports `OK` for all 18 non-`src/games/` files; `wc -l < src/main.rs` = 11 and `grep -c 'Command::' src/main.rs` = 0; the G10 `git diff f0b633f --stat -- docs/plan.md docs/component-evaluation.md artifacts/benchmarks artifacts/reproducibility/{corpus-determinism,corpus-diversity,annotation-determinism,selfplay-serial-vs-parallel}.md` is empty; `analyze` stdout SHA-256 identical with and without `-v` (`e34de1a5…`), `-v` stderr has 1 `INFO` line, plain stderr 0, `RUST_LOG=off` stderr 0 bytes; malformed TOML exits 2.
- Environment facts carried forward: worktrees under `C:/Users/John/Projects/worktrees/`; one cargo command per worker tool call; no `python` on PATH; Bash-tool commands longer than ~8 KB are truncated, so workers must use the Write tool for the PowerShell script and for all authored Markdown; `core.autocrlf = true`.

Phase 2 notes (decomposed 2026-08-22 at `0475db0`; every API signature quoted into a step was read from the live source at that commit):

- Dependency graph / waves: wave 1 = {11-cli-surface, 12-agreement} (disjoint: 11 owns `src/cli/**`, 12 owns `src/discovery/{agreement,analyze,mod}.rs`). Wave 2 = {13-play (after 11), 14-report (after 11 + 12)}. Wave 3 = 15-pipeline (after 11, 12, 14 — it calls `cli::analyze::run_analyze` and `cli::report::render_run_dir`). Wave 4 = 16-gate (report only).
- `src/cli/mod.rs` is edited by 11-cli-surface ONLY. That is the whole point of step 11: it fixes the command surface (adds `Play`/`Report`/`Pipeline` variants carrying `play::PlayArgs` / `report::ReportArgs` / `pipeline::PipelineArgs`, extends `Analyze` with `--game`/`--analyzers`/`--list-analyzers` and makes `--corpus` optional) and creates the three new module files as compiling placeholders, so steps 13/14/15 each own exactly one `src/cli/*.rs` file and never touch `mod.rs`. The three new modules are `pub mod` (not `mod`) because `Command` names their `Args` types and a private module would trip the `private_interfaces` lint under `-D warnings`.
- `tests/cli_pipeline.rs` is NOT touched in Phase 2 — every new integration test lands in a new file (`tests/cli_analyze.rs`, `tests/cli_play.rs`, `tests/cli_report.rs`, `tests/cli_agreement.rs`, `tests/cli_experiment.rs`), each owned by exactly one step. Acceptance blocks therefore never count `test result: ok` lines across the whole suite (that count changes as sibling steps merge, and Phase 1 already lost a step to exactly that mistake); they use per-binary counts (`cargo test --test <name> … | grep -c "<N> passed"`) plus `cargo test 2>&1 | grep -c "test result: FAILED"` → 0.
- Decomposer decisions filling gaps the brief left open (freeze these; Phase 3's README/ADR must document them as written):
  (a) `analyze` stdout: the frozen `games=… distinct_games=… canonical_coverage=… decisive_fraction=… diversity_pass=…` line is printed whenever the `summary` analyzer ran; when it did not, the stage prints `analyzers={comma list} checks_pass={bool} out={dir}` instead. `--strict` maps `AnalyzeMetadata::checks_pass == false` to `CliError::CheckFailed`, whose message keeps the frozen `diversity thresholds not met: …` wording when `summary` ran.
  (b) `analyze --list-analyzers` needs a game to instantiate a registry; it takes an optional `--game` and defaults to `KNOWN_GAMES[0]` (a reference, never a game-name literal — the "one game name, one file" rule).
  (c) `report --input <file>` picks the analyzer by the file STEM (`summary.json` → `summary`) and resolves the game from `run.json` in the file's own parent directory. No record schema gains a `game` field.
  (d) `pipeline`'s report stage writes `report_out` and prints `report={path} bytes={n}`; it does NOT echo the Markdown to stdout (only the standalone `report` command does).
  (e) `AGREEMENT_FILE` ("agreement.json") lives in `src/discovery/agreement.rs`, not `src/io/schema.rs` — `src/io/**` stays untouched in Phase 2. `ANALYZE_FILE` is still only reachable as `crate::io::schema::ANALYZE_FILE`.
  (f) `play`'s synthesized sweep uses opening name `play`, `strategies` = only the entries named by `--players` (first occurrence kept), `pairings` = one pairing in `--players` order, `evaluators` = `[--evaluator or bundle default]`, `random_opening_plies = [K]`. The equivalent TOML that must reproduce its bytes is embedded verbatim in steps 13 and 16.
  (g) `play` transcript format is frozen in step 13 (header `game {id} seed={:#018x} players={a,b} opening_plies={n}`, ply lines `{ply:>2}. {Player:?} {action-json}[ (opening)]`, final state `Display`-indented two spaces, `outcome={Debug or none} length={n}`, blank line after every game, totals `games={N} wins=X:{n},O:{m} draws={d} unfinished={u}`). Phase 3's README must copy it from a real run, not retype it.
- Emergent contracts Phase 3 must honor: `cli::analyze::run_analyze(bundle, corpus, &AnalyzeOptions) -> anyhow::Result<AnalyzeMetadata>` (runs analyzers, prints the stage line, raises `CheckFailed` under `strict`) and `cli::report::render_run_dir(bundle, dir, &AnalyzerRegistry<G>) -> Result<String, CorpusError>` are the two shared entry points the `pipeline` command composes; the run-directory report's section order is `# Run {run_id}` → header table → `## Annotation` (only when `annotate.json` exists) → one section per `analyze.json` entry in manifest order, blocks joined by exactly one blank line and the document ending in exactly one `\n`.
- Wall-time watch: the debug `cargo test` budget is 45 s and Phase 1 finished at 31.1 s. Each new integration-test file is capped at ONE `generate` of `tests/fixtures/generate-small.toml` (≈ 0.5 s/stage); `tests/cli_experiment.rs` runs the full experiment exactly once. If the gate measures a wall above 45 s, the fix is to fold tests together, not to raise the budget.
- Environment facts carried from Phase 1: worktrees under `C:/Users/John/Projects/worktrees/`; fresh-worktree debug `cargo test --no-run` ≈ 10 s; one cargo command per worker tool call; no `python` on PATH; Bash-tool commands longer than ~8 KB are truncated, so workers must write files with the Write/Edit tools, not heredocs; `cargo test` stderr carries ~23 `INFO`/`TRACE` lines from the lib test binary (never grep raw test stderr for arbitrary words).

Phase 1 notes (decomposed 2026-08-22 at `f0b633f`; every pin and every acceptance grep below was dry-run against the baseline binary):

- Dependency graph / waves: wave 1 = {01-cli-split, 02-analyze-registry, 03-corpus-helpers} in parallel (pairwise disjoint scopes). Wave 2 = 04-logging (after 01; edits `src/cli/mod.rs` — serialized behind the split) and 05-experiment (after 02; edits `src/discovery/mod.rs` — serialized behind 02's re-export edit; needs `AnalyzerRegistry` + `CorpusError::Precondition`). Wave 3 = 06-instrumentation (after 02, 03, 04; owns corpus.rs/annotate.rs/analyze.rs/minimax.rs + new `tests/cli_logging.rs`) and 07-exit-codes (after 02, 04; owns `src/cli/{error,mod,annotate,analyze}.rs`, `src/main.rs`, `tests/cli_pipeline.rs`) — 06 and 07 are disjoint and may run together. 08-gate last, report only. `src/cli/mod.rs` is edited by 01 → 04 → 07 in that order only; `src/discovery/mod.rs` by 02 → 05; `src/discovery/config.rs` only by 02 (adds `Precondition`); `src/discovery/analyze.rs` by 02 → 06; `src/discovery/corpus.rs` by 03 → 06.
- Environment facts: fresh worktree debug `cargo test --no-run` ≈ 10 s; 04-logging's `cargo build` pulls `matchers`, `aho-corasick`, `regex-automata 0.4.18`, `regex-syntax 0.8.11` into `Cargo.lock` (all four are now in the local registry cache; first fetch needed network). Git Bash has `sha256sum`; Bash tool commands longer than ~8 KB are truncated on this machine — workers must write files with the Write/Edit tools, not heredocs. Workers are told to use `cargo run --quiet -- …` (single cargo command, no extra stdout) and `target/stepNN/` scratch dirs.
- Emergent contracts (Phase 2/3 decomposer must honor): (a) `AnalyzerOutput { file: String, json: String, checks_pass: bool }` carries the EXACT file bytes (`AnalyzerOutput::new(file, &typed, checks_pass)` = `serde_json::to_string_pretty` + `\n`), NOT a `serde_json::Value` — `serde_json` is built without `preserve_order`, so a `Value` round-trip sorts keys and would break the pinned `summary.json` hash; `Analyzer::render(&self, &serde_json::Value)` still takes a `Value` (read back from the file) and its output STARTS with `## {Title}` (e.g. `## Summary`), subsections use `###`. (b) `analyze_outputs(bundle, dir, registry, options) -> (AnalyzeMetadata, Vec<AnalyzerOutput>)` is the pipeline-facing entry; `analyze(..)` discards the outputs; `analyze_corpus` is a wrapper over `["summary"]` and now also writes `analyze.json` (verified exact manifest bytes in step 02's acceptance; `strict` is recorded `false` by the wrapper until the Phase 2 `analyze` command calls `analyze()` with real options). (c) `load_corpus`/`LoadedCorpus`/`RunHeader` moved to `src/discovery/analyze.rs` as `pub` (report command can reuse). (d) `resolve_experiment` takes an explicit `registry: &AnalyzerRegistry<G>` parameter (brief's sketch omitted it; needed so Phase 1 tests can validate `agreement` via a stub and so later registrations are honored); `[generate]` is a REQUIRED section (no `#[serde(default)]` — would need `A: Default`); `annotate`/`analyze`/`report` sections default via manual `Default` impls + per-field `#[serde(default = "fn")]`; `[analyze.thresholds]` is all-or-nothing (partial table → TOML missing-field error). (e) `corpus::{RunWriter, play_cell, play_sweep}` are the shared generate/play path (`play_sweep(bundle, &resolved, &run_id, options, |cell, game, positions| ..)`); the `play` command (Phase 2) must resolve a one-cell `GenerateConfig`, compute `run_id = config_hash(&resolved.config)`, and feed the same sink order to a `RunWriter` for byte-identity with `generate`. (f) `cli::error::{CliError::{Usage, CheckFailed}, exit_code, EXIT_USAGE, EXIT_CHECK}`; `IoError::Json` classifies as exit 1; `CorpusError::Config|Precondition` → 2; strict failure stderr is `error: check failed: diversity thresholds not met: <failures joined by "; ">`. (g) `cli::games::{KNOWN_GAMES, dispatch_game!, game_of_toml_file, game_of_run_dir}` — `dispatch_game!(name, |bundle| expr)` evaluates `expr` (a `Result<T, E: From<CorpusError>>`) with `bundle: &GameBundle<G>`; the `play` command's `G::State: Display` bound goes on its generic helper fn, not on the macro. (h) `cli::logging::{init, level_name, filter_directive}`; `-v`/`-q` are `global = true` (accepted after the subcommand too). (i) Test-binary count after Phase 1 = 13 `test result: ok` lines (new `tests/cli_logging.rs`); `tests/cli_pipeline.rs` has 8 tests (strict test renamed `strict_analyze_exits_three_on_undiverse_corpus`).
- Pins re-verified at `f0b633f` (debug binary): generate-small `run_id=5eafa65f76637df3 cells=36 games=144 positions=1083`, annotate `annotated=390 terminal=0 disagreements=0`, analyze `games=144 distinct_games=115 canonical_coverage=0.312 decisive_fraction=0.549 diversity_pass=false`, six SHA-256s as in the brief; draws `run_id=933a94e742497e81 cells=1 games=16 positions=144`, analyze `games=16 distinct_games=1 canonical_coverage=0.013 decisive_fraction=0.000 diversity_pass=false`, `diversity_failures` = `canonical_coverage 0.01 < 0.50` / `decisive_fraction 0.00 < 0.20` / `distinct_game_fraction 0.06 < 0.50`; draws `summary.json` SHA-256 `ba46adfd6fa5a3c658e37d8bb54fd5b5d086f2e9c8078d26279e447fff4332d3` (not in the brief; usable as an extra pin). Baseline `cargo test` = 12 `test result: ok` lines (lib 253, main 0, 9 integration binaries, doctests 0).
- Probe results (throwaway test, reverted): `#[serde(untagged)] SweepSource<A> { Path(PathBuf), Inline(GenerateConfig<A>) }` works under toml 1.1.4 for both forms; an invalid `sweep` value reports `data did not match any variant of untagged enum SweepSource` (accepted; the brief's `sweep_path`/`sweep` fallback is NOT needed). `EnvFilter::new("info"|"off")` + `try_init()` twice behaves as expected (second returns `Err`, ignored); `EnvFilter::try_from_default_env()` errs when `RUST_LOG` is unset, hence `filter_directive` builds the directive string itself.

Planner notes for the decomposer:

- Baseline at `f0b633f`: `cargo test` = lib 253 + annotate 3 + cli_pipeline 6 + corpus 10 + corpus_bench 1 + match_bench 2 + match_engine 9 + minimax_tactics 7 + smoke 2 + ttt_bench 2 (295), debug wall ≈ 32 s; fmt/clippy clean. Pinned run_ids/SHA-256s in the brief's pin table were produced by the debug binary at this commit.
- `src/cli/mod.rs` is touched by the cli-split step and the exit-code step — serialize via `depends_on`, never in the same wave. `src/discovery/mod.rs` re-export edits: one step per phase.
- `tracing-subscriber` needs `features = ["env-filter"]` added in `Cargo.toml` (not present at baseline); `Cargo.lock` changes accordingly — that step owns both files.
- Verify `#[serde(untagged)]` `SweepSource` (string vs table) against `toml 1.1.4` with a throwaway test before baking fixture text into steps; fallback in the brief's Assumptions.
- Fixture-text mutations in tests must normalize CRLF→LF first (`core.autocrlf = true`).
- Workers: one cargo command per tool call; no `python` on PATH; worktrees under `C:/Users/John/Projects/worktrees/`; fresh worktree debug `cargo test --no-run` ≈ 10 s.

## Revisions

<!-- supervisor appends: phase | failed step | revision note | outcome -->

Phase 1 supervisor notes (wave 1 verified and merged 2026-08-22):

- `ANALYZE_FILE` is defined in `src/io/schema.rs` but NOT re-exported from `src/io/mod.rs` (that file was outside step 02's `files_in_scope`). Later steps and the Phase 2 decomposer must import it as `crate::io::schema::ANALYZE_FILE`, not `crate::io::ANALYZE_FILE`. Adding the re-export is optional cleanup for a step that already owns `src/io/mod.rs`.
- Accepted worker deviations, both minimal and in scope: step 03 added `#[allow(clippy::type_complexity)]` above `play_cell` (required by `-D warnings`; mirrors the existing attribute on `expand_game`); step 01 replaced the two `[`dispatch_game!`]` intra-doc links in `src/cli/games.rs` with plain code spans, because the macro is `pub(crate)` and rustdoc emitted `private_intra_doc_links` warnings that would have failed the zero-doc-warning gate.
- Integrated-tree verification after wave 1 (at `f9293a4`): `cargo test` = 12 `test result: ok` lines, 0 FAILED, lib 261 (253 baseline + 7 from step 02 + 1 from step 03); `cargo fmt --all --check`, `cargo clippy --workspace --all-targets --all-features -- -D warnings`, `cargo doc --no-deps --workspace` all clean; `generate`/`annotate`/`analyze` stdout unchanged and `run.json`/`games.jsonl`/`positions.jsonl`/`summary.json` SHA-256s all match the pins; `analyze --corpus` now also writes `analyze.json`.
- Wave 2 accepted deviation: step 05 used `enabled.then_some(AnnotateOptions { .. })` instead of the spec snippet's `enabled.then(|| AnnotateOptions { .. })` — clippy's `unnecessary_lazy_evaluations` rejects the closure form for a side-effect-free struct literal under `-D warnings`. Behaviorally identical. Step 04 had no deviations.
- Integrated-tree verification after wave 2 (at `523d7cc`): `cargo test` = 12 `test result: ok` lines, 0 FAILED, lib 271 (261 + 3 logging + 7 experiment); `cargo fmt --all --check` and `cargo clippy --workspace --all-targets --all-features -- -D warnings` clean; `generate` stdout byte-identical with and without `-v` (same stdout SHA-256), so the subscriber is stderr-only as required.

| phase | failed step | revision note | outcome |
| --- | --- | --- | --- |
| 1 | 07-exit-codes (pre-launch correction, option b) | Acceptance line 167 required `cargo test 2>&1 \| grep -c "test result: ok"` -> `13`, but 07 adds no new test binary. The 13th line comes from step 06-instrumentation's new `tests/cli_logging.rs`, and 06/07 are a parallel wave off the same base, so in 07's isolated worktree the honest count is 12 — the check was unsatisfiable by honest work and would have forced the worker to stop. Root cause: the decomposer wrote the post-merge integrated count into a per-worktree acceptance. Fix: acceptance corrected to `12` with a note that the 13th line is verified by the supervisor on the integrated tree after both steps merge. | corrected in flight before launch; step file committed |
- Known cosmetic wart (not blocking; for the Phase 2/3 decomposer to weigh): `cargo test` stderr now carries ~23 `INFO`/`TRACE` lines from the lib unit-test binary. Cause: `cli::logging::init_twice_is_harmless` installs the process-wide `tracing` subscriber, and `tracing-subscriber` writes through `io::stderr()`, which bypasses libtest's per-test capture, so later unit tests that call `generate`/`annotate`/`analyze` emit real log lines. Harmless — no line contains `FAILED`, and every `grep -c "test result: ok"` / `grep -c FAILED` count is exact. A fix would live in `src/cli/logging.rs` (e.g. `with_test_writer`, or dropping the subscriber install from that unit test); do NOT let a future step grep raw `cargo test` stderr for arbitrary words without accounting for these lines.
| 1 | 07-exit-codes (worker-resolved contradiction) | Action 7 named the two new tests `malformed_toml_exits_two` and `analyze_missing_corpus_exits_two`, but the acceptance block required `grep -c 'exits_two' tests/cli_pipeline.rs` -> `0`. Both names contain that substring, so the two requirements were mutually exclusive. Root cause: the decomposer added the anti-regression grep for the RENAMED strict test without noticing it also matches the new test names it had just specified. Fix: the worker kept the specified behavior and named them `malformed_toml_is_rejected` / `analyze_missing_corpus_is_rejected`, matching the file's existing convention. | accepted; behavior verified by the supervisor (cli_pipeline 8 passed, exits_two grep 0) |
| 1 | 08-gate (phase DoD not met; revision sent to decomposer) | Gate verdict FAIL on G8. Supervisor re-verified independently: `grep -rl tictactoe src --include=*.rs \| grep -v '^src/games/'` yields 17 files; 15 have all matches inside `#[cfg(test)]` and one is the allowed `src/cli/games.rs`, but `src/discovery/experiment.rs:8` is `//! game = "tictactoe"` — a module-doc TOML example at line 8, far above that file's `#[cfg(test)]` at line 395. The roadmap Phase 1 DoD requires that grep to return only `src/cli/games.rs` plus `#[cfg(test)]`-only matches, so the item fails as written. Root cause: step 05-experiment's `actions` explicitly ordered "a module doc (the TOML shape above as a ```toml block)" and that shape in its `context` contains `game = "tictactoe"` — the decomposer wrote the violation into the step, conflicting with the DoD. The worker followed its step exactly and is not at fault. NOT a planner amendment: honest work can satisfy the DoD (use a placeholder such as `game = "<game-name>"`, or point the doc at `configs/tictactoe-experiment.toml` instead of inlining a game name), so the DoD is sound and the step text is wrong. Every other Phase 1 DoD item verified green by the supervisor: 13 `test result: ok` / 0 FAILED / 341 passed, wall 39.5 s <= 45 s, fmt+clippy+doc clean, all six pinned `generate-small` SHA-256s reproduce (incl. `annotations.jsonl` 9ca6ede1... and `annotate.json` 65a73ac8...), draws pins + `--strict` exit 3 with exact stderr, all exit-2 cases, `main.rs` 11 lines with no command logic, registry/experiment/error/logging unit-test counts all exact. | revision note sent to decomposer for phase 1 |

Phase 1 revision notes (decomposed 2026-08-22 at `e22944e`; both new acceptance blocks dry-run against the real file before committing):

- Added `09-doc-game-neutral` (fixes the G8 violation) and `10-gate` (fresh DoD re-verification). `08-gate` is marked `superseded`, not removed: its commit `b2abe7f` and its honest `Overall verdict: FAIL` stay in history; only its verdict is replaced by `10-gate`'s. `05-experiment` stays `done` — it was merged and every other part of it is correct; only the one module-doc line it was instructed to write is corrected, by `09`.
- Dependency graph: `09-doc-game-neutral` (depends_on `05-experiment`, already merged) runs alone — it is the only pending non-gate step, so it may run in the working tree with no worktree. Then `10-gate` (depends_on `09`) runs alone, report-only. No parallelism remains in phase 1.
- Root cause recorded for future decomposition: step `05-experiment`'s `actions` ordered a module doc containing the `context`'s TOML shape, and that shape carried `game = "tictactoe"` — writing a roadmap-DoD violation directly into the step text. Lesson: the "one game name, one file" rule is a substring rule over `src/**/*.rs`; any step that mandates verbatim doc or code content must be checked against it before the step is committed.
- TRAP for any future step touching a game-neutral module: the rule greps for the bare substring `tictactoe`, so it also matches PATHS. A doc line pointing at `configs/tictactoe-experiment.toml` re-trips it. Reference `configs/` generically or use `tests/fixtures/experiment-small.toml`, whose filename is clean.
- Dry-run results on the pre-fix file that shaped `09`'s acceptance: `awk '/#\[cfg\(test\)\]/{if(!t) t=NR} /tictactoe/{if(!t) c++} END{print c+0}' src/discovery/experiment.rs` = `1` (becomes `0` after the fix); first `#[cfg(test)]` at line 395, first `tictactoe` at line 8; `grep -c tictactoe src/discovery/experiment.rs` = `10` before, must be `9` after (9 test-module occurrences preserved — this doubles as an anti-gaming check against "fixing" it by deleting test code); `grep -cF '//! [analyze.thresholds]'` / `'//! [generate]'` / `'//! [report]'` = `1` each, asserted to stay `1` so the documented TOML shape is not gutted. One candidate check was corrected during authoring: `grep -rl tictactoe src/discovery --include=*.rs | wc -l` stays `9`, NOT `8` — `experiment.rs` still matches at file level through its test module.

Phase 1 completion (supervisor, verified independently on the merged tree at `c8707ba`; every figure below re-measured by the supervisor, not copied from a gate report):

- DoD 1: `cargo build --workspace --all-targets` exit 0; `cargo test --workspace` = 13 `test result: ok` lines, 0 `FAILED`, 341 total passed (baseline 295); wall 31.1 s against the 45 s budget. `cargo fmt --all --check`, `cargo clippy --workspace --all-targets --all-features -- -D warnings`, `cargo doc --no-deps --workspace` all exit 0 with 0 warnings.
- DoD 2: banner `strategy-discovery 0.1.0` byte-exact; `generate`/`annotate`/`analyze` stdout lines unchanged; all six `generate-small` SHA-256s reproduce (run.json a5307c24, games.jsonl d0233f41, positions.jsonl cb8953df, annotations.jsonl 9ca6ede1, annotate.json 65a73ac8, summary.json 5794b37e); `analyze --corpus` also writes `analyze.json` listing `summary` -> `summary.json`.
- DoD 3: full G8 loop reports `OK` for all 16 non-`src/games/` files; `grep -rl tictactoe src/cli src/main.rs src/lib.rs` returns exactly `src/cli/games.rs`; `src/main.rs` 11 lines, 0 `Command::`.
- DoD 4: `builtin_registry_names_summary_only` 1 passed; `discovery::analyze::tests` 6 passed.
- DoD 5: `discovery::experiment::tests` 7 passed; both `configs/tictactoe-experiment.toml` and `tests/fixtures/experiment-small.toml` present.
- DoD 6: `cli::error` 22 passed; missing config -> 2, malformed TOML -> 2, empty corpus -> 2, unknown game -> 2; draws `analyze --strict` -> exit 3 with stderr exactly `error: check failed: diversity thresholds not met: canonical_coverage 0.01 < 0.50; decisive_fraction 0.00 < 0.20; distinct_game_fraction 0.06 < 0.50`.
- DoD 7: `tests/cli_logging.rs` 4 passed; `env-filter` enabled in `Cargo.toml`; stdout SHA-256 identical with and without `-v` for ALL THREE stages (generate, annotate, analyze), confirming logging is stderr-only.
- DoD 8: `phase6-cli-10-gate-report.md` `Overall verdict: PASS`, 13/13 rows PASS, 0 FAIL rows.
- Wave/merge history: wave 1 {01,02,03} -> wave 2 {04,05} -> wave 3 {06,07} in worktrees under `C:/Users/John/Projects/worktrees/`, each merged with `merge(phase6-cli): step <id>`; 08/09/10 ran in-tree (lone steps, no worktree). All worktrees removed and all `wt/` branches deleted.
- Carry-over for Phase 2: import `ANALYZE_FILE` as `crate::io::schema::ANALYZE_FILE` (not re-exported from `src/io/mod.rs`); `cargo test` stderr carries ~23 `INFO`/`TRACE` lines from the lib test binary (see the earlier note) — do not grep raw test stderr naively; the "one game name, one file" rule is a SUBSTRING rule over `src/**/*.rs` and also matches paths, so never name `configs/tictactoe-experiment.toml` inside a game-neutral `.rs` file.

Phase 2 completion (supervisor, verified independently on the merged tree at `589eb9e`; every figure below re-measured by the supervisor, not copied from the gate report):

- Waves/merges: wave 1 {11-cli-surface, 12-agreement} and wave 2 {13-play, 14-report} ran in worktrees under `C:/Users/John/Projects/worktrees/`, each merged with `merge(phase6-cli): step <id>`; 15-pipeline and 16-gate ran in-tree (lone steps, no worktree). All worktrees removed and all `wt/` branches deleted.
- DoD 1 (brief items 2,3,5,6,7): full chain `generate -> annotate -> analyze --analyzers summary,agreement -> report` on `generate-small` all exit 0 and the run directory holds all eight files; `analyze --list-analyzers` prints exactly `agreement` and `summary` lines; `agreement.json` `by_strategy` has `perfect` rate `1.0` (296/296) and `random` rate `0.6795366795366795` (176/259) — also `depth-2` 0.949, `opening` 1.0, `random-opening` 0.694.
- DoD 2: `play --game tictactoe --players random,perfect --games 2 --seed 7 --out <dir>` exits 0, totals line `games=2 wins=X:0,O:1 draws=1 unfinished=0`; `run.json`/`games.jsonl`/`positions.jsonl` byte-identical (SHA-256) to `generate` on the equivalent sweep TOML (`run_id=988795b19ac38e3c cells=1 games=2 positions=15`); `annotate --corpus` and `analyze --corpus` on that directory both exit 0.
- DoD 3: `report --input <dir>` stdout has exactly one each of `# Run 5eafa65f76637df3`, `## Annotation`, `## Summary`, `## Agreement`; zero `|---` occurrences; 0 heading-blank-line violations and 0 table-blank-line violations (awk scan); ends with exactly one `\n`; two consecutive runs SHA-256 `314a13bb33f83b6a82ac9f0b05e89f170c7a2a9388f9f7797254b1efe51ff9b2` both times. `report --input <dir>/summary.json` exits 0, starts `## Summary`, contains no `# Run ` and no `## Agreement`.
- DoD 4: `pipeline --experiment tests/fixtures/experiment-small.toml --out <dir>` exits 0, last line `pipeline=experiment-small out=<dir> stages=generate,annotate,analyze,report`, directory holds the eight files plus `report.md`; `--stages analyze` on an empty dir exits 2 with `error: precondition failed: <dir>\run.json not found; run the `generate` stage first`; a synthesized draws experiment with `strict = true` exits 3 with `error: check failed: diversity thresholds not met: canonical_coverage 0.01 < 0.50; decisive_fraction 0.00 < 0.20; distinct_game_fraction 0.06 < 0.50`.
- DoD 5: all six `generate-small` SHA-256 pins reproduce (run.json a5307c24, games.jsonl d0233f41, positions.jsonl cb8953df, annotations.jsonl 9ca6ede1, annotate.json 65a73ac8, summary.json 5794b37e); `generate-draws` run_id `933a94e742497e81`; `cargo test` = 18 `test result: ok` lines, 0 `FAILED`, 360 passed (Phase 1 end: 341), warm debug wall 34 s against the 45 s budget; `cargo fmt --all --check`, `cargo clippy --workspace --all-targets --all-features -- -D warnings`, `cargo doc --no-deps --workspace` all exit 0 with 0 warnings. Exit codes: unknown analyzer 2, missing config 2, unknown game 2, `analyze --strict` on draws 3. `-v analyze` stdout SHA-256 identical to plain (`e34de1a5...`) with `INFO` only on stderr. `grep -rl tictactoe src --include=*.rs | grep -v '^src/games/'` -> every listed file has 0 pre-`#[cfg(test)]` matches except the allowed `src/cli/games.rs`; `src/main.rs` 11 lines.
- DoD 6: `phase6-cli-16-gate-report.md` `Overall verdict: **PASS**`, 14/14 rows PASS.
- Accepted worker deviations, all minimal and in scope: step 12 named the analyzer by module path instead of repeating the type name in `builtin_registry`'s doc, because a second `AgreementAnalyzer` occurrence would have made the step's `grep -c "AgreementAnalyzer" src/discovery/analyze.rs` acceptance return 2; steps 14 and 15 replaced intra-doc links to `pub(crate)` items with plain code spans (`rustdoc::private_intra_doc_links` would have failed the zero-doc-warning gate — same wart Phase 1 hit in step 01); step 15 collapsed `if stages.contains(&"annotate") { if let Some(options) = .. }` into one `&& let` condition, forced by clippy's `collapsible_if` under `-D warnings`.
- Carry-over for Phase 3: `pipeline`'s report line prints a platform-native path (`report=<dir>\report.md bytes=3195` on Windows) — README examples must be copied from a real run, and any determinism script must not compare that line across platforms. `pipeline` on `experiment-small` prints `diversity_pass=true` (its thresholds are 0.25/0.2/0.5), while the standalone `analyze --corpus` on the same corpus prints `diversity_pass=false` (defaults 0.5/0.2/0.5) — both are correct; do not "fix" one to match the other. `play`'s equivalent sweep has `run_id=988795b19ac38e3c` (a new pin, usable in the determinism evidence). The frozen `play` transcript above was produced at `589eb9e` and is what README must copy.
